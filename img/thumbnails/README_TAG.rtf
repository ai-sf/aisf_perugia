 Per cambiare le immagini sotto Thumbnails ,
 si deve cambiare URL altrimenti la modifica non viene caricata.
  dunque se modifica un\'92immagine cambia (per esempio) il nome
  della cartella che la contiene e rinomina(con lo stesso nome)
  la voce img: della pagina(a cui punta l\'92URL che si vuole condividere) }
